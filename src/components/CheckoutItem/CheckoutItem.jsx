function CheckoutItem({pizza}) {
    // call the pizza holder reducer to find the same pizza that 
        // matches the index of the one in the order form

    // use this function to map it?
        // thoughts are hard
    const findPizzaToDisplay = () => {
        // loop through reducer data to find pizza with id that
            // matches the pizza.id prop given
    }

    return (
        <tr>
            <td>Pizza Name!</td> 
            <td>Cost!!!</td>
        </tr>
    )
}

export default CheckoutItem;